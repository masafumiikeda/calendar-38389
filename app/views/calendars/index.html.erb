<%= link_to '予定の追加', new_calendar_path %>
<%= month_calendar calendars: @calendars do |date, calendars| %>
  <%= date.day %>

    <% calendars.each do |calendar| %>
    <div>
      <%= calendar.title %>
    </div>
  <% end %>
<% end %>